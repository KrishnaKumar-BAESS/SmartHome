# Source references

`house-inventory.xlsx` is the original root `Book1.xlsx`, renamed and moved
without changing its bytes. It is reference material, not an automatically imported
application database. The app currently reads `../apps/web/src/data/house.ts`.
